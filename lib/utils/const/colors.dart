import 'package:flutter/material.dart';

/// Palette BotRoad V2 — copilote IA de mobilité.
class AppColors {
  // Accent principal — Corail BotRoad
  static const Color primary = Color(0xFFF56B6F);

  // Variantes accent
  static const Color primaryLight = Color(0xFFFF8D92);
  static const Color primaryDark = Color(0xFFE85A5F);

  // Alias legacy (migration progressive)
  static const Color accent = Color(0xFFFF8D92);
  static const Color accentLight = Color(0xFFFFB3B6);
  static const Color accentDark = Color(0xFFE85A5F);

  // Fonds
  static const Color background = Color(0xFFF8F6F4);
  static const Color backgroundSecondary = Color(0xFFF0EEEC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceElevated = Color(0xFFF8F6F4);

  // Texte
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textMuted = Color(0xFF999999);

  // Système
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3498DB);

  // Neutres
  static const Color neutral = Color(0xFF999999);
  static const Color neutralLight = Color(0xFFEAEAEA);
  static const Color neutralDark = Color(0xFF444444);

  // Bordures & effets
  static const Color divider = Color(0xFFEAEAEA);
  static const Color accentSoft = Color(0xFFFFE7E8);
  static const Color glow = Color(0x26F56B6F);
}
